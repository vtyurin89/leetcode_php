class Solution {

    /**
     * @param String $s
     * @return Boolean
     */
    function isPalindrome($s) {
        $s = trim(strtolower(preg_replace('/[^a-zA-Z0-9]/', '', $s)));
        return $s === strrev($s);    
    }
}

